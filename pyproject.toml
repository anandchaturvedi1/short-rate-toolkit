[tool.poetry]
name = "short-rate-toolkit"
version = "0.1.0"
description = "Short-rate models (Vasicek, CIR, Ho-Lee, Hull-White) with MC simulation"
authors = ["Anand Chaturvedi"]

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.25"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
